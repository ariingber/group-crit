<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Document</title>
</head>
<body>
  <div>
    <a href="/users/new">Signup</a>
  </div>
  <div>
    <a href="/users/login">Login!</a>
  </div>
  <div>
    <form method="post" action="users/logout?_method=DELETE">
      <button>logout</button>
    </form>
  </div>

  <% if (user) {%>
    <h3><%= user.name %> you are logged in.</h3>
  <% } %>

  <% if (user) {%>
    <h3><a href= <%= "/users/" + user.id %> >Go to your page</a></h3>
  <% } %>
</body>
</html>
